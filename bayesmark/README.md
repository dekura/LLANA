1. ```./configs``` contains the points used for initialization (of LLANA and baselines), e.g. ```configs/{model_name}/{seed}.json``` is the configuration for the ```seed``` run for ```model_name```.

2. ```./data``` contains data files.